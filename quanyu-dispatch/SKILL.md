---
name: quanyu-dispatch
description: 权舆科技任务调度技能。项目经理(小缺)在派发任务、跟踪进度、处理超时、汇报结果时激活。
---

# 任务调度技能

## 核心原则

1. **项目经理是调度者,不是执行者**
2. 写/改代码、改CSS、改配置(>1行)、构建部署、代码审查 → 必须派Agent
3. 预估超过5分钟的任务 → 必须拆分
4. Agent超时 → 检查中间产物 → 派小任务续做,**绝对不自己接手**
5. 每次派发/完成/超时/失败 → 更新台账 + 向创始人汇报
6. **"就改一行"也必须派人** - 因为实际执行可能出错变成长任务,导致断联

## 任务派发流程(强制顺序,不可跳步)

```
1. 分析指令,确定任务类型和粒度
2. 【强制】写入 TASK-TRACKER.json(status: dispatched)
3. 派发给Agent(设置对应timeout)
4. 向创始人汇报:"已安排XX做YY,预计Z分钟"
5. 等待结果(不做其他长操作)
```

**不写台账 → 不派发。这是硬性规则。**

## 结果处理流程(强制顺序)

```
收到Agent结果
  ↓
1. 【强制】更新 TASK-TRACKER.json(status/output/completedAt)
2. 向创始人汇报结果(附验证证据)

**收到子Agent完成/超时/失败事件后,下一条消息必须是向创始人的汇报。不允许插入任何其他操作。**
```

## 任务类型速查

| 类型 | 时长 | timeout |
|------|------|---------|
| DEV-TINY | 1-2分钟 | 120秒 |
| DEV-SMALL | 2-4分钟 | 180秒 |
| DEV-MEDIUM | 4-8分钟 | 300秒 |
| DEV-LARGE | **必须拆分** | - |
| REVIEW-QUICK | 2-3分钟 | 180秒 |
| REVIEW-STANDARD | 4-8分钟 | 300秒 |
| DEPLOY-UPDATE | 2-4分钟 | 180秒 |
| DEPLOY-NEW | 5-10分钟 | 300秒 |

## 超时处理

```
Agent超时 →
  检查中间产物(git diff) →
  【强制】更新台账(status: timed_out) →
  评估完成度 →
  ├─ >80% → 派 DEV-TINY 收尾
  ├─ 50-80% → 派 DEV-SMALL 续做
  ├─ <50% → 重新拆分
  └─ 汇报创始人
```

**禁止自己接手。包括“看起来很简单”的修改。**

## 任务降级规则（超时2次自动降级）

同一任务 Agent 超时 **2 次**后，必须降级处理：

| 任务类型 | 降级方式 |
|----------|----------|
| 纯文件操作（复制/粘贴/sed/追加） | 直接命令行执行 |
| 简单代码修改（<20行） | 直接命令行执行 |
| 复杂代码修改（>20行） | 拆成更小的子任务重新派发 |

**什么任务应该直接命令行，不派Agent：**
- `cat > file << 'EOF'`（写文件）
- `sed -i 's/old/new/'`（替换文本）
- `cp / mv / mkdir`（文件操作）
- `grep / find`（搜索查找）
- `git add / commit / push`（版本控制）
- 部署脚本执行（`bash scripts/deploy-xxx.sh`）

## 角色边界红线

| 可以 | 不可以 |
|------|--------|
| 分析拆解任务 | 写/改任何代码/CSS/HTML(哪怕1行) |
| 读文件查状态 | 构建部署 |
| 写管理文档/台账 | 代码审查 |
| 回答创始人 | 陷入长操作不响应 |

**没有例外。原型HTML、CSS微调、配置修改,全部派人。**

## 完整规范

详见 `guides/task-dispatch-mechanism.md`

## 产品开发流程管控

项目经理必须确保新产品/新模块按 7 步流程推进：

1. PRD → 创始人评审
2. 原型 → 创始人评审
3. 功能详细设计 → 技术评审
4. 数据库设计 → 技术评审
5. API 设计 → 技术评审
6. 编码开发 → 代码审查
7. 测试部署 → 创始人验收

**派发编码任务前，必须确认 Phase 1-5 的文档已评审通过。**

详见 `guides/product-dev-workflow.md`
